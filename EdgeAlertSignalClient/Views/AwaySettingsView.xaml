<UserControl x:Class="EdgeAlertSignalClient.Views.AwaySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EdgeAlertSignalClient.Views"
             xmlns:vm="clr-namespace:EdgeAlertSignalClient.ViewModels"
             xmlns:converters="clr-namespace:EdgeAlertSignalClient.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AwaySettingsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Theme/MenuButtonTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#E2F0D9" CornerRadius="5" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" 
                       Text="Away Timeout Configuration" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,25"
                       Foreground="#004166"/>

            <!-- Description -->
            <TextBlock Grid.Row="1"
                       Text="Configure how long a user must be inactive before being marked as 'Away'. This setting applies globally to all EdgeAlert users."
                       TextWrapping="Wrap"
                       Margin="0,0,0,25"
                       Foreground="#004166"/>

            <!-- Input Section -->
            <Border Grid.Row="2" 
                    Background="#CDEAFB" 
                    Padding="20" 
                    Margin="0,0,0,20" 
                    CornerRadius="6"
                    BorderBrush="#004166"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="Timeout Minutes:" 
                               FontWeight="SemiBold"
                               FontSize="16"
                               Foreground="#004166"
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Row="0" Grid.Column="1"
                             Text="{Binding AwayTimeoutMinutes, UpdateSourceTrigger=PropertyChanged}"
                             Width="100"
                             Height="40"
                             FontSize="16"
                             Margin="15,0,0,0"
                             Padding="10,5"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center"
                             BorderBrush="#004166"
                             BorderThickness="1"/>

                    <TextBlock Grid.Row="0" Grid.Column="2"
                               Text="(Value between 1-60 minutes)"
                               Foreground="#004166"
                               FontStyle="Italic"
                               VerticalAlignment="Center"
                               Margin="15,0,0,0"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                               Text="This setting defines how many minutes of inactivity before a user is marked as 'Away'."
                               Foreground="#004166"
                               FontStyle="Italic"
                               Margin="0,15,0,0"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        Margin="0,10,0,10">
                <Button Content="Save Changes"
                        Command="{Binding SaveSettingCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                        Width="150"
                        Height="60"
                        Margin="0,0,15,0"
                        Style="{StaticResource SupportButtonTheme}"/>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>