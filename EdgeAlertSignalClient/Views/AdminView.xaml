<UserControl x:Class="EdgeAlertSignalClient.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EdgeAlertSignalClient.Views"
             xmlns:converters="clr-namespace:EdgeAlertSignalClient.Converters"
             mc:Ignorable="d"
             FontSize="16"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Theme/TabTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="#E2F0D9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Administration Panel"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="20,20,0,10"
                   Foreground="#004166"/>

        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Away Settings">
                <ContentControl Content="{Binding AwaySettingsView}"/>
            </TabItem>

            <TabItem Header="System Configuration">
                <ContentControl Content="{Binding SystemConfigView}"/>
            </TabItem>
            <TabItem Header="User Management">
                <ContentControl Content="{Binding UserManagementView}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>