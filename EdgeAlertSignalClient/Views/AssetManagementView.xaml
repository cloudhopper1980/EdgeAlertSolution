<UserControl x:Class="EdgeAlertSignalClient.Views.AssetManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:EdgeAlertSignalClient.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <!-- Master List -->
        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Assets}"
                  SelectedItem="{Binding SelectedAsset, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding AssetType}"/>
                <DataGridTextColumn Header="Serial" Binding="{Binding SerialNumber}"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="ODS" Binding="{Binding OdsLocation}"/>
                <DataGridTextColumn Header="Assigned To" Binding="{Binding AssignedTo}"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Detail Panel -->
        <StackPanel Grid.Column="1" Margin="10" DataContext="{Binding SelectedAsset}">
            <TextBlock Text="Asset Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Type:" Width="100"/>
                <TextBox Text="{Binding AssetType}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Serial Number:" Width="100"/>
                <TextBox Text="{Binding SerialNumber}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Model:" Width="100"/>
                <TextBox Text="{Binding Model}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Status:" Width="100"/>
                <TextBox Text="{Binding Status}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ODS Location:" Width="100"/>
                <TextBox Text="{Binding OdsLocation}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Assigned To:" Width="100"/>
                <TextBox Text="{Binding AssignedTo}" IsReadOnly="True" Width="200"/>
            </StackPanel>
            <!-- Add more fields as needed -->
        </StackPanel>
    </Grid>
</UserControl>
